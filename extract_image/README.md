# PDF Image Extraction with fitz and PIL

## Overview

This project demonstrates how to extract images from PDF documents using Python libraries `fitz` for PDF parsing and `PIL` for image processing.
